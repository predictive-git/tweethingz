#!/bin/bash

DIR="$(dirname "$0")"
. "${DIR}/config"

export DB_INSTANCE_IP=$(gcloud sql instances describe ${DB_INSTANCE} \
    --format='value(ipAddresses[0].ipAddress)')

echo "Configuring ${APP_NAME} schema in ${DB_INSTANCE} (${DB_INSTANCE_IP})..."

mysql --host $DB_INSTANCE_IP \
      --user $APP_NAME \
      --password=$(<"${DB_CONF_DIR}/app_password") \
      $APP_NAME < etc/schema.ddl

